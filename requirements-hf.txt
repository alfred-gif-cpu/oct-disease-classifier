# Dependency set verified to load the trained models correctly.
# Differs from requirements.txt: TF bumped to 2.16.x (Keras 3, matches the
# saved .h5 format), numpy pinned <2 for TF ABI, CPU/headless variants for
# a slim server image. Only the packages app.py actually needs at runtime.
Flask==3.0.0
gunicorn==21.2.0
tensorflow-cpu==2.16.1
numpy==1.26.4
opencv-python-headless==4.8.1.78
scikit-learn==1.3.2
scikit-image==0.22.0
Pillow==10.1.0
joblib==1.3.2
h5py==3.10.0
